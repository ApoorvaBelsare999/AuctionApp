<%= form_with(model: item, local: true) do |form| %>
  <% if item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :item_name %>
  </div>

  <div class="field">
    <%= form.label :bidcost %>
    <%= form.text_field :bidcost, id: :item_bidcost %>
  </div>

  <div class="field">
    <%= form.label :bidder %>
    <%= form.text_field :bidder, id: :item_bidder %>
  </div>

  <div class="field">
    <%= form.label :user %>
    <%= form.text_field :bidder, id: :item_bidder %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
